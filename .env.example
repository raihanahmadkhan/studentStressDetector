# API Configuration
# Set the backend API URL (default: http://localhost:8000)
VITE_API_URL=http://localhost:8000

# Local Mode
# Set to 'true' to force client-side calculation without backend
# If not set or 'false', will try API first and fallback to local on error
# VITE_USE_LOCAL=false
