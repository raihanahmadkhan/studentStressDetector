# No frontend environment variables are required.
# Requests use /api on the browser's origin: Vite proxies locally and
# netlify.toml proxies production traffic to Render.
# OAuth credentials and session secrets belong only in backend environment variables.
